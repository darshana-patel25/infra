{% comment %} {% include 'breadcrumb' %} {% endcomment %}

<div class="grid page_content">

  <div class="grid-item {% if page.handle == 'returns-shipping' %}page_shipping_returns{% endif %} large--seven-tenths push--large--one-sixth">

    <h1 class="{% if page.handle == 'returns-shipping' %}hide{% endif %}page_title">{{ page.title }}</h1>

    <div class="rte">
      {{ page.content }}
    </div>

  </div>

</div>
